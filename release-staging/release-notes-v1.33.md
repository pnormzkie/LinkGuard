Verdicts no longer contradict their own risk score.

• Vendor counts are real — "Flagged by 1 security vendor", not "multiple".
• Coverage is honest — no more "local rules only" when most checks did run.
• The domain-age check works again after failing silently on every scan.
• Fewer false alarms on ordinary links (3 in 10 before, 0 in 10 after).
• In-app updates keep working after GitHub moved its download host.

451 tests, plus a new 35-case accuracy corpus. Updates in place over v1.32 — your scan history is kept.
