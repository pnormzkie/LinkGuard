<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Required to show the emergency popup over other apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Required to launch the install prompt for downloaded app updates -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Needed to discover the real browser when forwarding safe links -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="LinkGuard"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinkGuard"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.QrScannerActivity"
            android:exported="false" />

        <!-- Link interception: scans tapped links before they reach the browser -->
        <activity
            android:name=".ui.LinkInterceptActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.LinkGuard.Intercept">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Threat Alert Activity (The Emergency Popup) -->
        <activity
            android:name=".ui.ThreatAlertActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showForAllUsers="true"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />

        <!-- Scan Detail Activity -->
        <activity
            android:name=".ui.ScanDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- History Activity -->
        <activity
            android:name=".ui.HistoryActivity"
            android:exported="false"
            android:label="Scan History"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Setup / Permissions Activity -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" />

        <!-- Notification Listener Service -->
        <service
            android:name=".service.LinkNotificationService"
            android:label="LinkGuard Protection"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Receiver for notification actions -->
        <receiver
            android:name=".util.DismissReceiver"
            android:exported="false" />

    </application>

</manifest>
